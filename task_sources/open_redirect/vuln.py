def is_safe_redirect(url):
    # Учебная функция проверки redirect URL.
    ### БЛОК РЕДАКТИРОВАНИЯ ###
    # УЯЗВИМО: внешний URL тоже считается безопасным.
    return bool(url)
    ### БЛОК РЕДАКТИРОВАНИЯ ###
