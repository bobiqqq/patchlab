# Защита заметок от XSS

Маршрут `/note` выводит параметр `note` прямо в HTML-ответ.

## Цель
Экранируйте содержимое `note` перед выводом, чтобы закрыть XSS.

## Подсказка
Можно использовать `html.escape()` или похожую функцию экранирования.
