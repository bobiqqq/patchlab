def can_open_admin_panel(user):
    # Учебная функция проверки доступа к админ-панели.
    ### БЛОК РЕДАКТИРОВАНИЯ ###
    # УЯЗВИМО: доступ даётся по имени, а не по роли пользователя.
    return user.get("username") == "admin"
    ### БЛОК РЕДАКТИРОВАНИЯ ###
